Claude Code sessions now defer MCP tool schemas instead of loading every one into the context window up front, which fixes the context-fill and auto-compact loop some users hit. Updates are quieter: they install in the background and only ask for a restart. Plus startup, Windows, and error-reporting fixes.
